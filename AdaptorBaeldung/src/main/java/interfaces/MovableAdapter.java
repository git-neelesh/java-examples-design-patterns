package interfaces;

/**
 * dapter interface MovableAdapter that will be based on the same Movable class.
 * It may be slightly modified to yield different results in different scenarios:
 */
public interface MovableAdapter {
  double getSpeed();
}
